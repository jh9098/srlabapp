루트의 AGENTS.md와 docs/01~07을 모두 읽고 작업해라.
이번 작업 범위는 backend_api 초기 구조 생성까지다.

해야 할 일:
1. FastAPI 프로젝트 기본 구조 생성
2. SQLAlchemy/Alembic 세팅
3. 아래 테이블 모델 생성
   - stocks
   - price_levels
   - support_states
   - watchlists
4. 초기 마이그레이션 생성
5. README에 로컬 실행 방법 추가

하지 말 것:
- Flutter 코드 수정
- admin_web 구현
- 푸시 기능 구현
- 결제/커뮤니티 기능 추가

작업 후:
- 변경 파일 목록
- 구현 내용 요약
- 남은 TODO
- 실행 방법
을 정리해라.



---------------


루트의 AGENTS.md와 docs/01~07을 다시 읽고 작업해라.
이전 작업에서 생성한 backend_api 구조와 DB 모델, 마이그레이션을 기준으로 이어서 구현해라.

이번 작업 범위는 핵심 MVP 백엔드 API 구현이다.

해야 할 일:
1. 아래 API 구현
   - GET /api/v1/stocks/search?q=
   - GET /api/v1/stocks/{stock_code}
   - GET /api/v1/watchlist
   - POST /api/v1/watchlist
   - DELETE /api/v1/watchlist/{watchlist_id}
   - PATCH /api/v1/watchlist/{watchlist_id}/alert
2. docs/05_지지저항Lab_DB_API_통합명세서.md의 공통 응답 구조를 반영
   - success
   - message
   - data
   - error_code
3. 종목 상세 API에서 아래 구조를 최소 포함
   - stock
   - price
   - status
   - levels
   - support_state
   - scenario
   - reason_lines
   - watchlist
4. 더미 또는 seed 데이터가 부족하면 로컬 테스트 가능한 최소 seed 데이터 추가
5. 핵심 API에 대한 최소 테스트 추가
   - 종목 검색
   - 종목 상세
   - 관심종목 추가/삭제/알림 변경

하지 말 것:
- Flutter 코드 수정
- admin_web 구현
- 푸시 기능 구현
- themes API 구현
- notifications API 구현
- 결제/커뮤니티 기능 추가

중요:
- API는 화면 중심으로 구현
- route handler에 비즈니스 로직을 과하게 넣지 말고 service/repository로 분리
- docs의 상태명과 필드명을 가능한 한 유지
- 불명확한 부분은 추측으로 큰 기능을 추가하지 말고 TODO로 남겨라

작업 후 반드시 정리할 것:
1. 변경 파일 목록
2. 구현 내용 요약
3. 실행 방법
4. 테스트 방법
5. 남은 TODO


---------------


루트의 AGENTS.md와 docs/01~07을 다시 읽고 작업해라.
이전 단계에서 구현된 backend_api를 기준으로 지지선 상태엔진과 이벤트 생성을 이어서 구현해라.

이번 작업 범위는 support state engine + signal events + theme/home 기초 API 구현이다.

해야 할 일:
1. docs/03_지지저항Lab_지지선상태관리_로직명세서.md를 기준으로 support state 계산 서비스 구현
2. 아래 상태 전이 로직 구현
   - WAITING → TESTING_SUPPORT
   - TESTING_SUPPORT → DIRECT_REBOUND_SUCCESS
   - TESTING_SUPPORT → BREAK_REBOUND_SUCCESS
   - DIRECT_REBOUND_SUCCESS → REUSABLE
   - BREAK_REBOUND_SUCCESS → REUSABLE
   - DIRECT_REBOUND_SUCCESS → INVALID
   - BREAK_REBOUND_SUCCESS → INVALID
3. 상태 변화 시 signal_events 생성 로직 구현
4. 동일 상태/동일 이벤트 중복 생성 방지 로직 추가
5. 아래 API 구현
   - GET /api/v1/home
   - GET /api/v1/stocks/{stock_code}/signals
   - GET /api/v1/themes
6. home API는 최소 아래 데이터 포함
   - market_summary
   - featured_stocks
   - watchlist_signal_summary
   - themes
   - recent_contents
7. support state engine 단위 테스트 추가
   - 직접 반등 성공
   - 이탈 후 반등 성공
   - 재활용 가능
   - 무효화
8. signal event 생성 테스트 추가

하지 말 것:
- Flutter 코드 수정
- admin_web 구현
- FCM 푸시 연동
- 실시간 전종목 수집 인프라 대규모 구현
- 결제/커뮤니티 기능 추가

중요:
- 상태 계산 파라미터는 하드코딩만 하지 말고 최소한 설정 가능한 형태를 고려해라
- docs의 상태명, signal_type, 응답 구조를 유지해라
- home API는 MVP 화면이 바로 붙을 수 있는 형태로 반환해라
- 차트 데이터는 최소 수준으로만 유지해라

작업 후 반드시 정리할 것:
1. 변경 파일 목록
2. 구현 내용 요약
3. 상태엔진 설계상 가정한 부분
4. 테스트 방법
5. 남은 TODO


------------
루트의 AGENTS.md와 docs/01~07을 다시 읽고 작업해라.
이전 단계에서 구현된 backend_api를 기준으로 frontend_app Flutter MVP 화면을 구현해라.

이번 작업 범위는 Flutter 핵심 화면 구현과 백엔드 API 연동이다.

해야 할 일:
1. frontend_app Flutter 프로젝트 구조를 feature 기반으로 정리
2. 아래 핵심 화면 구현
   - 홈
   - 관심종목
   - 종목 검색
   - 종목 상세
   - 테마
   - 마이(최소 버전)
3. 하단 탭 구조 구현
   - 홈
   - 관심종목
   - 테마
   - 쇼츠
   - 마이
4. 아래 공통 UI 컴포넌트 구현
   - stock card
   - status badge
   - loading state
   - empty state
   - error state
5. 아래 API 연동 구현
   - GET /api/v1/home
   - GET /api/v1/stocks/search
   - GET /api/v1/stocks/{stock_code}
   - GET /api/v1/watchlist
   - POST /api/v1/watchlist
   - DELETE /api/v1/watchlist/{watchlist_id}
   - PATCH /api/v1/watchlist/{watchlist_id}/alert
   - GET /api/v1/themes
6. 종목 상세 화면에 최소 아래 요소 구현
   - 상단 가격 정보
   - 상태 배지
   - 지지선/저항선 카드
   - 시나리오
   - 해설 3줄
7. 관심종목 추가/삭제/알림 토글이 실제로 동작하게 구현
8. 네트워크 로딩/빈 상태/에러 상태 처리
9. 앱 실행 방법을 README에 보강

하지 말 것:
- admin_web 구현
- FCM 푸시 구현
- 알림함 구현
- 내부 영상 플레이어 구현
- 고급 차트 도구 구현
- 결제/커뮤니티 기능 추가

중요:
- UI를 과하게 화려하게 만들 필요 없다
- MVP 목적은 “종목 상태를 빠르게 확인”하는 것이다
- 백엔드 응답 구조에 맞춰 모델/DTO를 분리해라
- 하드코딩 데이터보다 실제 API 연동을 우선하되, 필요한 경우 fallback mock은 최소로만 사용해라
- docs/02의 화면 목적과 우선순위를 최대한 지켜라

작업 후 반드시 정리할 것:
1. 변경 파일 목록
2. 구현된 화면 목록
3. 실행 방법
4. 아직 미완성인 화면/기능
5. 남은 TODO


-----------------


루트의 AGENTS.md와 docs/01~07을 다시 읽고 작업해라.
이전 단계에서 구현된 backend_api와 frontend_app을 기준으로 admin_web과 notifications/push 기초를 마무리해라.

이번 작업 범위는 관리자 기능 + 알림함 + 푸시 기반 구조 + 최종 MVP 정리다.

해야 할 일:
1. admin_web 또는 기존 관리자 영역에 아래 기능 구현
   - 종목 관리
   - 가격 레벨 관리
   - 지지선 상태 조회
   - 신호 이벤트 조회
   - 상태 강제 수정
   - 홈 노출 관리(최소 버전)
   - 테마 관리(최소 버전)
   - 운영 로그 조회
2. backend_api에 관리자용 API 구현 또는 보강
   - 종목 등록/수정
   - 가격 레벨 등록/수정
   - 지지선 상태 강제 수정
   - 신호 이벤트 조회
   - 홈 노출 데이터 수정
   - 테마 등록/수정
3. notifications 관련 API 구현 또는 보강
   - GET /api/v1/notifications
   - PATCH /api/v1/notifications/{notification_id}/read
   - GET /api/v1/me/alert-settings
   - PATCH /api/v1/me/alert-settings
4. frontend_app에 알림함 화면과 알림 설정 화면 구현
5. 푸시 연동의 기반 구조 구현
   - 디바이스 토큰 저장 구조
   - signal event → notification 저장 흐름
   - 실제 푸시 provider 연동은 최소 골격 또는 FCM 연동 가능한 구조까지
6. signal_events 발생 시 notifications가 저장되도록 연결
7. admin의 수동 수정/수동 푸시 관련 audit log 저장
8. README 업데이트
   - backend_api 실행 방법
   - frontend_app 실행 방법
   - admin_web 실행 방법
   - 환경변수 예시
9. docs와 실제 코드가 어긋나는 부분이 있으면 README 또는 TODO로 명확히 정리

하지 말 것:
- 결제 기능 추가
- 커뮤니티 기능 추가
- 자동매매/증권사 주문 연동
- 과도한 실시간 스트리밍 고도화
- MVP 범위를 넘는 추천 시스템 추가

중요:
- 관리자 기능은 “예쁘게”보다 “운영 가능하게” 구현해라
- 수동 상태 수정은 반드시 로그가 남아야 한다
- notifications와 signal_events는 분리해서 다뤄라
- FCM 등 실제 푸시 연동이 완전히 어렵다면, 최소한 provider 교체 가능한 구조와 notification 저장 흐름은 반드시 구현해라
- 미완성 영역은 TODO로 숨기지 말고 명확히 남겨라

작업 후 반드시 정리할 것:
1. 변경 파일 목록
2. 구현된 관리자 기능 목록
3. 구현된 알림/푸시 범위
4. 실제 남아있는 TODO
5. 전체 MVP 완료 여부 판단
6. 로컬 실행 방법